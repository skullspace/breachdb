* Add a 'groupby' and 'sum' to db.rb, and look at where that should be used in PasswordCache.list calls to make sure we don't end up with duplicate rows all over the place
* Add '^' and '$' to search operations
* Make the 'download' links work
# Remove c_hash_count from password table and see if anything breaks
* Take submissions

